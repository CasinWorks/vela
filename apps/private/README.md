# Vela Private — Parent Portal

Studio-concept umbrella site for private aviation and luxury ground travel.

Sibling apps: Vela Aviation (`localhost:3001`) and Vela Concierge (`localhost:3002`).

Override URLs with `VITE_PARENT_URL`, `VITE_AVIATION_URL`, `VITE_CONCIERGE_URL`.
